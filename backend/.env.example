# Required in production. Use a long random value.
JWT_SECRET=replace-with-a-long-random-secret

# Optional. Defaults to the backend directory locally.
DATA_DIR=.

# Optional for split frontend/backend hosting. Use comma-separated origins.
CLIENT_ORIGIN=http://localhost:3000

# Optional. Most hosts provide PORT automatically.
PORT=5000
