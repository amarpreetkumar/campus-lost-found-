# Leave empty when Express serves the React build from the same deployed service.
# For split hosting, set this to your backend origin, for example:
# REACT_APP_API_URL=https://campus-lost-found-api.onrender.com
REACT_APP_API_URL=
